[build]
  command = "npm run build"
  publish = "_site"

[build.environment]
  NODE_VERSION = "18"

# Redirect /admin to admin page
[[redirects]]
  from = "/admin"
  to = "/admin/index.html"
  status = 200
